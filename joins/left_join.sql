-- ============================================================================
-- TOPIC: LEFT JOIN (LEFT OUTER JOIN)
-- ============================================================================
-- LEFT JOIN returns ALL rows from the left table, and matched rows from the
-- right table. If there is no match, NULL values are returned for right table
-- columns.
-- ============================================================================

-- ----------------------------------------------------------------------------
-- PRACTICE SECTION: Write your own LEFT JOIN queries below
-- ----------------------------------------------------------------------------
-- TODO: Perform a basic LEFT JOIN — list all departments and their employees.
-- TODO: Use LEFT JOIN with WHERE IS NULL to find departments with no employees.
-- TODO: Use LEFT JOIN with an aggregate (COUNT) to count employees per department (including 0).
-- TODO: Filter a LEFT JOIN with a condition on the left table (e.g., location = 'New York').
-- TODO: List all departments and their employees (include departments with 0 employees).
-- TODO: Find employees who may not belong to any department (if any).
-- TODO: Show department names and the total salary paid by each, including empty departments.

